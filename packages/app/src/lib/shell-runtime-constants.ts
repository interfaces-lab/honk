/** Dispatched when workspace / shell cwd changes (legacy string preserved for listeners). */
export const SHELL_LAYOUT_CHANGED_EVENT = "pi-shell-layout-changed";
/** Dispatched when seeding composer text from the host (legacy event name for listeners). */
export const COMPOSER_EDITOR_SET_EVENT = "pi-composer-editor-set";
